# المرحلة 0 — خط أساس البناء ومراجعة G0

## الحالة السابقة

- مشروع الاختبارات كان خارج `FishFarmManager.sln`.
- لم توجد `[Fact]` أو `[Theory]`، لذلك لم يكن `dotnet test` دليلاً فعلياً.
- فشل بناء سابق بسبب رفض الكتابة إلى ملفات `obj` الحالية.

## تغييرات المرحلة 0

- إضافة مشروع الاختبارات إلى الحل.
- استبعاد `Tests/**/*.cs` من تجميع التطبيق الرئيسي.
- إضافة اختبار دخان ينشئ SQLite مؤقتة ويحفظ عميلاً.

## نتيجة التشغيل

| الفحص | النتيجة | الدليل |
|---|---|---|
| restore | ناجح | استعيد المشروعان من مخرجات نظيفة |
| Release build | ناجح: 0 أخطاء، 18 تحذيراً | تشغيل 12 يوليو 2026 |
| test discovery | ناجح: ملف واحد واختبار واحد | xUnit/VSTest |
| tests passed | 1/1 ناجح | `DatabaseSmokeTests.CanCreateSchemaAndPersistCustomer` |

أثناء التحقق اكتشف الاختبار تعارض EF Core 9 في مشروع الاختبار مع EF Core/SQLite 8 في التطبيق. أزيلت حزمة InMemory 9 غير المستخدمة وأعيد الاختبار بنجاح. كما أصلح قفل ملف SQLite الاختباري بتعطيل pooling لاتصال الاختبار.

## بوابة G0

**القرار الحالي:** مجتازة بتاريخ 13 يوليو 2026 — يسمح ببدء G1 فقط. دليل القرار التفصيلي في `PHASE0_G0_CLOSURE.md`.

| المعيار | الهدف | الحالة |
|---|---|---|
| بناء Release نظيف | نجاح | متحقق: 0 أخطاء و18 تحذيراً |
| اختبار آلي مكتشف | ≥1 ناجح | متحقق: 1/1 |
| نطاق R1 | معتمد | متحقق: اعتماد PO-01 مسجل |
| قرار DB والنشر | موثق | مقبول مرحلياً |
| كل Must له ID/مالك/قبول | 100% | متحقق: 21/21 في `PHASE0_ACCEPTANCE_MATRIX.md` |
| سجل مخاطر | كل ≥15 له معالجة | متحقق: مالك ومعالجة واستحقاق في `PHASE0_RISK_ACTIONS.md` |
| Definition of Done | معتمد | متحقق: اعتماد PO-01 مسجل |
| جرد الوظائف | مصنف | خط أساس أولي |

## شروط الإغلاق المنفذة في 13 يوليو 2026

1. اعتمد مالك المنتج `PO-01` نطاق R1 بتوجيه إغلاق G0.
2. ثُبت سجل هويات المساءلة في `PHASE0_OWNERS_AND_APPROVALS.md`، مع إلزام تعيين المختصين قبل بواباتهم.
3. أضيفت معايير قبول تفصيلية لكل Must بنسبة 100% في `PHASE0_ACCEPTANCE_MATRIX.md`.
4. حُددت تواريخ معالجة جميع المخاطر في `PHASE0_RISK_ACTIONS.md`.

## قيد بيئة المراجعة

مجلد المشروع يسمح بتعديل ملفات موجودة وإنشاء بعض الملفات المباشرة، لكنه رفض إنشاء مجلد `Docs/Phase0` والكتابة إلى `obj` الحالي. لذلك حفظت أدلة المرحلة كملفات `Docs/PHASE0_*`، وسيستخدم البناء مجلد مخرجات نظيفاً أو تعالج ACL قبل اعتماد G0.
