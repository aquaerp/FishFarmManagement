# المرحلة 1 — الأساس التقني والأمن الأولي

**آخر تحديث:** 14 يوليو 2026  
**حالة التنفيذ التقني:** مكتمل  
**قرار البوابة G1:** غير مجتازة تشغيليًا — بانتظار خمسة تشغيلات CI خضراء متتالية وتكوين نسخة خارج الجهاز.

## ما نُفذ

- توحيد مسار SQLite عبر `RuntimePaths` وإزالة الإنشاء النسبي الصامت لـ`fishfarm.db`.
- إضافة فحوص بدء التشغيل للمجلدات والصلاحيات والمساحة والإعدادات الإنتاجية.
- منع Demo في Production ومنع تضمين `DataSeeder` في Release.
- إلزام Demo في Debug بسر خارجي قوي عبر `AQUAFARM_DEMO_PASSWORD`.
- استبدال النسخ المباشر بـSQLite Online Backup.
- تشفير النسخ بـWindows DPAPI وكتابة SHA-256 checksum والتحقق من `PRAGMA integrity_check`.
- إضافة pre-restore backup وrollback تلقائي عند فشل الاستعادة.
- نجاح ثلاث استعادات متتالية في اختبار آلي.
- إضافة قفل حساب محفوظ في قاعدة البيانات وmigration، مع BCrypt وترقية SHA256 القديمة عند الدخول الصحيح.
- تشفير ZATCA API Key عند التخزين باستخدام DPAPI.
- إضافة معالج أخطاء مركزي يعرض رقم تتبع دون كشف تفاصيل الاستثناء للمستخدم.
- تحديث حزم NuGet وإزالة جميع الثغرات المعروفة التي أبلغ عنها المصدر الرسمي.
- إضافة فحوص CI للثغرات والأسرار وسياسة حزمة Release.

## أدلة التحقق

| الفحص | النتيجة في 14 يوليو 2026 |
|---|---|
| Restore/Release build | ناجح، 0 أخطاء و18 تحذيرًا |
| الاختبارات الآلية | 6/6 ناجحة |
| تمرين الاستعادة | 3/3 متتالية ناجحة |
| تشفير النسخ وchecksum | ناجح ومختبر |
| migrations الأمنية | ناجحة ومختبرة |
| NuGet vulnerability audit | لا حزم ضعيفة معروفة |
| فحص الأسرار المضمنة | ناجح |
| فحص حزمة Release من بيانات Demo | صفر علامات Demo |

## عناصر البوابة المتبقية

1. ربط المستودع بمنصة CI وتشغيل workflow خمس مرات متتالية بنجاح. المستودع المحلي لا يملك remote حاليًا.
2. تحديد مسار خارجي فعلي في `Backup:ExternalDirectory` ثم ضبط `Backup:ExternalCopyRequired=true` وتنفيذ تمرين فصل الجهاز.

لا يمنع هذان العنصران مواصلة تحسينات G1 محليًا، لكنهما يمنعان تسجيل البوابة `G1 = Passed`.

